{
  "name": "espaces-ouverts",
  "version": "1.0.0",
  "description": "Espaces Ouverts - Association Directory",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix theme/static_src && npm run build --prefix theme/static_src"
  },
  "engines": {
    "node": "24.x",
    "npm": "10.x"
  }
}
